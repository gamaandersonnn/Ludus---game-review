services:
  db:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: qwqwe
    ports:
    - "3306:3306"
    volumes:
    - ludus_data:/var/lib/mysql

volumes:
  ludus_data: